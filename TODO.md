# TODO

Живой список задач по развитию `mcp-russia`.

## Актуальные задачи

- [ ] Углублять документацию примеров и справочных материалов.
- [ ] Добавлять новые модули данных (ФМС, Роснедвижимость, Росатом и др.).
- [ ] Расширять клиентские тесты на неполные и изменившиеся схемы внешних API.
- [ ] Проверять доступность ЕМИСС-кодов через сетевые контрактные тесты.

## Выполнено в раунде 2026-08-14 (сто восемнадцатый проход — актуализация MCP-метаданных, русификация остатков)

- Исправлен английский остаток в промптах Делопроизводства: `prompt` → `промпт`, `resource` → `ресурс` (8 вхождений в `agenty/deloproizvodstvo/prompts.py`).
- Переведены английские заметки в `data/state.json`: «Telegram is not configured.» → «Telegram не настроен.» (4 вхождения).
- Исправлена опечатка латиницы в теге РосАПИ: `"фиаc"` (лат. c) → `"фиас"` (кирилл. с) в `rosapi/__init__.py` и `rosapi/server.py` (3 вхождения).
- Обновлены описания модулей для точного отражения источников данных:
  - Росгидромет: «Данные Росгидромета» → «Погодные и климатические данные по России через Open-Meteo» — честная атрибуция реального API.
  - Минобрнауки: «Данные Минобрнауки России» → «Данные Минобрнауки и Рособрнадзора» — отражает оба источника (obrnadzor.gov.ru + vuz.minobrnauki.gov.ru).
- Обновлены теги Росгидромета: убраны `"росгидромет"`, `"спутники"` (данные не от Росгидромета); добавлены `"open-meteo"`, `"качество-воздуха"`.
- Обновлены теги Минобрнауки: добавлен `"рособрнадзор"`, убраны `"наука"`, `"исследования"` (не отражают фактические данные).
- Обновлены теги Делопроизводства: добавлены `"гост"`, `"шаблоны"`.
- Повышены версии зрелых модулей с 0.1.0 до 0.2.0: ЦБ РФ, МЧС, МВД, Россельхознадзор, Ростехнадзор, Делопроизводство.
- Версия корневого сервера обновлена: 0.5.0 → 0.6.0.
- Обновлены инструкции корневого сервера: уточнены источники данных (Росгидромет — через Open-Meteo, Минобрнауки/Рособрнадзор).
- Синхронизирована документация: `features.md`, `CONTRIBUTING.md`, `README.md` — уточнены описания Росгидромета и Минобрнауки.
- Полный аудит MCP-метаданных 27 модулей: имена, версии, описания, теги, FastMCP-параметры — рассинхронизаций не обнаружено (кроме исправленных выше).
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 850 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-13 (сто семнадцатый проход — актуализация ссылок: ADR-001/002 → CONTRIBUTING.md, исправление тега Счётной палаты)

- Заменены все 48 устаревших ссылок на несуществующие документы ADR-001 и ADR-002 на актуальную ссылку CONTRIBUTING.md в 27 модулях (26 данных + 1 агентный) и `_shared/feature.py`. Ранее корневой `server.py` был обновлён, но модули оставались на старой ссылке.
- Исправлен ошибочный тег «росфинмониторинг» в метаданных Счётной палаты (`rosaudit/__init__.py`): Росфинмониторинг — отдельное ведомство, не относящееся к Счётной палате. Замена: «росфинмониторинг» → «государственный-аудит».
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 850 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-12 (сто шестнадцатый проход — актуализация MCP-метаданных: instructions, version, исправление опечаток)

- Все 27 модулей (26 данных + 1 агентный) теперь передают `instructions` (из `META_FUNKTSII.opisanie`) и `version` (из `META_FUNKTSII.versiya`) в конструктор `FastMCP()`. Ранее MCP-протокол не получал описаний подмодулей.
- Корневой сервер `mcp-russia` дополнен `instructions` с перечислением всех 26 модулей данных и агентного модуля, а также `version="0.5.0"`.
- Исправлена опечатка в описании ЦБ РФ: «золотовалютые» → «золотовалютные» резервы.
- Исправлена рассинхронизация имени модуля КАД Арбитраж: `mcp-russia-kad-arbitrazh` (дефис) → `mcp-russia-kad_arbitrazh` (подчёркивание, как в `imya` и имени каталога).
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 850 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-11 (сто пятнадцатый проход — зачистка португальских оригиналов, русские PyPI-ключевые слова, уточнение README)

- Удалены оставшиеся португальские идентификаторы из CHANGELOG.md: `partidos_senado` → Партии Федерального сената, `ufs_senado` → Федеративные единицы Сената, `jurisprudencia` → Судебная практика Национального совета юстиции, `TREs` → Региональные избирательные суды, `TJMs` → суды военной юстиции.
- Удалены квалификаторы «бразильская»/«бразильского» из описаний удалённых модулей в CHANGELOG.md: «бразильская ПНЗК» → «Национальная платформа государственных закупок», «бразильского портала госзакупок» → «портала госзакупок».
- Заменён английский идентификатор `_enrich_candidate_names` на описание «устаревшая функция» в CHANGELOG.md (функция из удалённого модуля).
- Добавлены русские PyPI-ключевые слова в `pyproject.toml`: `государственные-данные`, `открытые-данные`, `публичные-данные`, `русскоязычный`.
- Уточнена формулировка в README.md: «257 русскоязычных инструментов и ресурсов» → «257 русскоязычных инструментов, 90 ресурсов и 62 промпта» (257 — только инструменты, не сумма).
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 850 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-11 (сто четырнадцатый проход — актуализация метаданных, удаление португальских оригиналов)

- Исправлены устаревшие счётчики в README.md: «256» → «257» инструментов и ресурсов; «24 клиентских модуля» → «26 клиентских модулей».
- Исправлен комментрий в settings.py: «257+» → «257» (точное количество).
- Обновлена ссылка в docstring server.py: «по конвенции ADR-001/002» → «по конвенции CONTRIBUTING.md».
- Удалены португальские оригиналы из CHANGELOG.md: убраны `порт. …` скобки с диакритикой (Plataforma Nacional de Contratações Públicas, Partidos do Senado Federal, Unidades Federativas do Senado, Jurisprudência do CNJ, Tribunais Regionais Eleitorais, Tribunais de Justiça Militar); удалена ссылка Compras.gov.br. Русские переводы сохранены.
- Консолидирован список актуальных задач: выполненные пункты убраны, оставлены 4 направления развития.
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 850 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-10 (сто тринадцатый проход — инструменты Росстата, взвешенная агрегация, ресурсы и промпты)

- Клиентские функции `poluchit_inflyatsiyu` и `poluchit_demografiyu` переведены на типизированные Pydantic-схемы `InflyatsiyaDannye` и `DemografiyaDannye` вместо `list[dict[str, Any]]`.
- Инструмент `demografiya` теперь выводит колонку «Ест. прирост» (поле `estestvenny_prirost` схемы `DemografiyaDannye`).
- Удалён мёртвый код: `client.poluchit_indikator`, `_razobrat_otvet_indikatora` и неиспользуемая схема `PokazatelRosstata`. Все инструменты используют `poluchit_indikator_dannye`.
- Добавлен инструмент **поиск региона** (`poisk_regiona`): поиск субъекта РФ по подстроке названия (без учёта регистра).
- Добавлены 3 ресурса модуля Росстата: `data://pokazateli` (справочник показателей), `data://okved` (разделы ОКВЭД 2), `data://subiekty-rf` (субъекты РФ с кодами и ФО).
- Добавлены 3 промпта модуля Росстата: `analiz_vrp_regionov` (сравнительный анализ ВРП), `obzor_truda` (обзор рынка труда), `analiz_demografii` (анализ демографии).
- Добавлена **взвешенная агрегация** в `sravnenie_okrugov`: для относительных показателей (`zarplata`, `bezrabotitsa`, `dokhody_na_dushu`, `uroven_bednosti`, `vrp_na_dushu`, `realnaya_zarplata`, `selkoe_khozyaystvo`, `estestvennyy_prirost`, `migratsiya`) используется взвешенное среднее (вес — население региона), для абсолютных — сумма.
- Добавлен справочник `NASELENIE_SUBIEKTOV` в constants.py: приблизительная численность населения по субъектам РФ (2024).
- Добавлено множество `OTNOSITELNYE_POKAZATELI` в constants.py для определения типа агрегации.
- Обновлён промпт `analiz_regiona`: шаг 1 теперь предлагает `poisk_regiona()` как альтернативу `spisok_regionov()`.
- Обновлена документация: `features.md` — 22 инструмента Росстата, 5 ресурсов, 5 промптов; 257 инструментов, 90 ресурсов, 62 промпта всего.
- Добавлены 10 тестов: `poisk_regiona` (3 теста), взвешенная агрегация (2), константы (3), схемы (2).
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и все тесты проходят без ошибок.

## Выполнено в раунде 2026-08-10 (сто двенадцатый проход — русификация кодов ФО, новые инструменты Росстата)

- Заменены латинские коды федеральных округов на русские аббревиатуры (ЦФО, СЗФО, ЮФО, СКФО, ПФО, УФО, СФО, ДФО) в 9 файлах constants.py: Росстат, Минздрав, Роспотребнадзор, Минобрнауки, Ростехнадзор, МВД, МЧС, Россельхознадзор.
- Обновлены ключи словарей статистики (po_fo) в МВД и МЧС: tcentralnyy→ЦФО, severo-zapadnyy→СЗФО и т.д.
- Упрощён инструмент sravnenie_okrugov: удалён словарь sokrashcheniya_okrugov (маппинг ЦФО→CFO), теперь коды ФО используются напрямую.
- Обновлены преобразования код→название в МВД (_kod_v_nazvanie_fo) и МЧС (.replace() цепочка).
- Россельхознадзор: krymskiy→КФО (Крымский федеральный округ).
- Исправлены прочие нерусифицированные элементы:
  - ГИБДД: Tm→Тм, Tb→Тб (категории водительских удостоверений — официальные русские обозначения).
  - РосАПИ: PATENT→PSN (Патентная система налогообложения — русская аббревиатура).
  - Минздрав: pharma→farmatsevticheskaya, radio→radiatsionnaya (русская транслитерация).
  - РосАПИ: исправлена битая ссылка adres_dannyeess→address.
- Добавлены 6 выделенных инструментов Росстата для ключевых показателей:
  - `vvp_dannye` — ВВП России (ЕМИСС 60201).
  - `bezrabotitsa_dannye` — уровень безработицы (ЕМИСС 43062).
  - `dokhody_na_dushu` — среднедушевые денежные доходы (ЕМИСС 57039).
  - `promyshlennoe_proizvodstvo` — индекс промышленного производства (ЕМИСС 43045).
  - `uroven_bednosti` — уровень бедности (ЕМИСС 33460).
  - `srednyaya_pensiya` — средний размер пенсий (ЕМИСС 60440).
- Добавлены сетевые контрактные тесты для 14 обновлённых ЕМИСС-кодов (test_emiss_kody_dostupny). Запускаются при MCP_RUSSIA_NETWORK_TESTS=1.
- Обновлена документация: features.md — 21 инструмент Росстата, 256 инструментов всего; settings.py — 256+ инструментов.
- Добавлены 12 тестов для новых инструментов Росстата (2 теста на инструмент: с данными и без).
- Линтер, форматирование, строгая типизация (mypy: 0 ошибок в 233 файлах) и 841 тест проходят без ошибок.

## Выполнено в раунде 2026-08-07 (сто одиннадцатый проход — актуализация метаданных, исправление рассинхронизаций документации)

- Исправлена структура секции Россельхознадзор в `features.md`: таблица расширена с 2 до 9 инструментов, добавлены отсутствовавшие строки (`spisok_vidov_narusheniy`, `spisok_tipov_produktsii`, `poisk_proverok`, `poisk_karantinnykh_obektov`, `poisk_registratsiy_produktsii`, `veterinarsnye_sertifikaty`, `preduprezhdeniya_karantina`).
- Устранены дублирующиеся секции Resources/Prompts/Authorization в конце `features.md` (остатки от незавершённой вставки секции МВД).
- Добавлен заголовок секции МВД (`### mvd`), отсутствовавший после предыдущей правки.
- Обновлён заголовок `features.md`: 27 модулей · 250 инструментов · 87 ресурсов · 59 промптов (фактические числа из сервера).
- Исправлено количество модулей во всех файлах документации: 24 → 26 модулей данных.
  - `README.md`: «24 активных» → «26 активных», обновлён список модулей, убраны дублирующие строки МЧС и Россельхознадзора.
  - `CONTRIBUTING.md`: «24 модуля» → «26 модулей», добавлены МВД и Ростехнадзор в дерево каталогов.
  - `docs/index.md`: «24 российских модуля» → «26 российских модулей» (2 места).
  - `docs/guide/development.md`: «Все 24 модуля» → «Все 26 модулей».
- Исправлено количество инструментов в `settings.py`: «все 154+ инструментов» → «все 250+ инструментов».
- Обновлён раздел README.md «Направление развития»: убраны выполненные задачи (добавление МВД/Рособрнадзор/Ростехнадзор, строгая типизация, нормализация, журналирование ошибок, генерация справочника, сетевые контрактные тесты, метаданные авторизации).
- Добавлен агентный модуль Делопроизводство в список README.md «Что уже сделано».

- Обновлены все 27 ЕМИСС-кодов показателей Росстата на актуальные (5–6-значные). Переход на 9xxxxxx не состоялся — ЕМИСС продолжает использовать 5–6-значные коды; в 2016–2017 гг. Росстат перешёл с ОКВЭД-2007 на ОКВЭД 2, и показатели получили новые коды с данными с 2017 г. Старые коды (24xxx, 27xxx, 31xxx) заморожены — данные по 2016 г.
- Основные замены: ИПЦ 31088→31074, население 24133→31557, ВВП 26973→60201, промышленность 30826→43045, безработица 24139→43062, зарплата 24140→58701, розничная торговля 31082→31260, инвестиции 24145→33644, с/х 30955→34128, строительство 31106→57605, ВРП 26975→61497, ВРП на душу 26976→61483, реальная зарплата 24142→43245, доходы на душу 24141→57039, бедность 24143→33460, субсидии 24144→31610, миграция 24134→61749, естественный прирост 24135→59561, коэффициент Джини 24146→58804, средняя пенсия 24147→60440, жильё 31103→40454, внешнеторговый оборот 31099→37150, грузооборот 31221→60373, наука 27621→62466, структура ВРП 27103→59450.
- Добавлен инструмент **сравнение федеральных округов** (`sravnenie_okrugov`): рейтинг федеральных округов по выбранному показателю. Агрегирует региональные данные по округам.
- Расширен справочник показателей (`KLYUCHEVYE_INDIKATORY`): добавлены 6 новых мнемонических кодов (vrp_na_dushu, migratsiya, estestvennyy_prirost, zhile, subsidii, investitsii_po_vidam), всего 27 показателей.
- Обновлён инструмент `pokazateli_rosstata`: теперь выводит колонку с кодом ЕМИСС для каждого показателя.
- Обновлён промпт `analiz_regiona`: добавлены шаги для использования `informatsiya_o_regionye()` и `sravnenie_okrugov()`.
- Обновлён ресурс `metodologiya`: добавлены упоминания ОКВЭД 2 и замороженных старых кодов.
- Обновлены EMISS-коды в `OTRASLEVAYA_STRUKTURA_VRP` и `REGIONALNYE_POKAZATELI`.
- Обновлены fallback-ссылки в инструментах (tools.py) и клиенте (client.py) на новые ЕМИСС-коды.
- Обновлены тесты: старые коды (31088, 24140, 27103, 24145) заменены на актуальные; добавлены 3 теста для `sravnenie_okrugov`.
- `features.md` обновлён: 14 инструментов Росстата, 245 инструментов всего. Добавлен `sravnenie_okrugov`, обновлены описания.
- `README.md` обновлён: отражены обновлённые ЕМИСС-коды и новый инструмент сравнения округов.

## Выполнено в раунде 2026-08-06 (сто девятый проход — новые модули, расширение инструментов, сетевые контрактные тесты)

- Добавлен модуль **МВД России** (`mvd`): 8 инструментов — справочники (виды преступлений, виды ДТП, федеральные округа, наборы открытых данных), статистика преступности и ДТП, данные розыска, наркотические преступления. CSV-разбор открытых данных data.gov.ru и мвд.рф. Резервные статические данные за 2024. 3 ресурса, 2 промпта.
- Добавлен модуль **Ростехнадзор** (`rostekhnadzor`): 7 инструментов — справочники (виды надзора, классы опасности, виды лицензий, виды инцидентов), поиск инцидентов/аварий на ОПО, поиск лицензий, реестр опасных производственных объектов. CSV-разбор открытых данных data.gov.ru. Резервные статические данные за 2024. 3 ресурса, 2 промпта.
- Расширен модуль **Рособрнадзор** (`minobrnauki`): 4 новых инструмента — вузы с запретом приёма (`vuzy_zapret_priema`), статистика контрольной деятельности (`statistika_kontrolya`), результаты проверок (`rezultaty_proverok`), реестр экспертов (`reestr_ekspertov`). Открытые данные obrnadzor.gov.ru (контроль, проверки, эксперты). Версия обновлена до 0.3.0.
- Добавлен параметр `avtor` (фильтр по автору) и `sootvetstvie` (поиск по тексту названия) в инструмент `zakonoproekty` модуля Госдумы. Клиент `poluchit_zakonoproekty` передаёт `author`/`searchString` в API СОЗД.
- Добавлен инструмент **поимённое голосование** (`poimennoe_golosovanie`) в модуль Совета Федерации: возвращает результаты голосования сенаторов (ФИО, регион, голос, фракция).
- Добавлен инструмент **лесной надзор** (`poisk_lesnogo_nadzora`) в модуль Росприроднадзора: поиск проверок лесного надзора с фильтрацией по организации, региону, году. Вид надзора `lesnoy` добавлен в справочник `VIDY_NADZORA`.
- Добавлен инструмент **реестр недобросовестных поставщиков** (`poisk_rnp`) в модуль Закупок: поиск в РНП по ИНН, названию, году. Новая схема `ZapisRNP` с полями включения, основания, органа-заказчика.
- Добавлены инструменты **бюджетная классификация** в модуль Казначейства: `spisok_razdelov_byudzheta` (14 разделов БК с числовыми кодами), `spisok_podrazdelov_byudzheta` (33 подраздела), `byudzhetnaya_smeta` (смета по номеру). Параметры `razdel`/`podrazdel` в `ispolnenie_byudzheta` для фильтрации по разделам/подразделам БК.
- Добавлены опциональные сетевые контрактные тесты (`tests/test_network_contracts.py`): 5 тестов доступности источников и схем JSON-ответов. Запускаются только при `MCP_RUSSIA_NETWORK_TESTS=1`.
- Файл `autowork.log` добавлен в `.gitignore` и исключён из отслеживания git (13 МБ журнала с португальским содержимым).
- `features.md` обновлён: 26 модулей, 244 инструмента. Добавлены секции МВД и Ростехнадзора, новые инструменты в 5 модулях.
- `README.md` обновлён: 26 модулей, МВД и Ростехнадзор в списке источников.
- Линтер, форматирование и 823 теста проходят без ошибок.

## Выполнено в раунде 2026-08-05 (сто восьмой проход — актуализация MCP-метаданных)

- Исправлены все устаревшие ссылки на несуществующие инструменты: `cbrf_kursy_valyut` → `cbrf_tekushchie_kursy` (server.py), `gosduma_poluchit_deputatov` → `gosduma_spisok_deputatov` (planner.py, test_discovery.py), `gosduma_raskhody_deputata` → `gosduma_info_deputata` (planner.py, test_discovery.py), `rosstat_poluchit_indikator` → `rosstat_indikator_dannye`, `rosstat_poluchit_dannye_regiona` → `rosstat_informatsiya_o_regionye` (planner.py).
- Примеры в системном промпте планировщика переписаны с реальными инструментами и параметрами.
- Исправлены опечатки транслитерации в features.md: `spisok_tipov_licenziy` → `spisok_tipov_litsenziy`, `info_licenzii` → `info_litsenzii`, `poisk_licenziy` → `poisk_litsenziy` (ц = ts).
- Расширена проверка документации (test_documented_tools.py): добавлены 4 теста — проверка голых имён инструментов в smart-tools.md и примерах, проверка вызовов в docstring исходного кода, проверка количества инструментов в features.md, проверка имён инструментов в features.md по локальному реестру модулей. Всего 6 тестов документации.
- Добавлена машиночитаемая пометка `*(планируемый)*` в features.md для планируемых инструментов; тесты документации автоматически исключают помеченные строки из проверки.
- Добавлено поле `operatsii_trebuyut_avtorizatsii` в `MetaFunktsii`: список имён инструментов, требующих авторизованный API. Госдума: `spisok_deputatov`, `zakonoproekty`, `golosovaniya`; Закупки: `poisk_zakupok`, `poisk_kontraktov`, `info_zakupki`.
- Каталог инструментов (discovery.py) теперь аннотирует операции, требующие авторизацию: `[требует MCP_RUSSIA_DUMA_API_TOKEN]`.
- В features.md инструменты с авторизацией помечены `*(рекомендуется API-токен)*`.
- Добавлен скрипт `scripts/generate_tool_reference.py` для генерации полного Markdown-справочника из MCP-реестра с параметрами, тегами и статусом авторизации.
- Линтер, форматирование, строгая типизация (`mypy`: 0 ошибок в 217 файлах) и 784 теста проходят без ошибок.

- Создан `_shared/normalizatsiya.py`: шесть типизированных помощников для безопасного приведения значений внешних API — `bezopasnaya_stroka`, `bezopasnoe_tseloe`, `bezopasnoe_chislo`, `izvlech_spisok`, `pervoe_znachenie`, `razorvat_stroku_spisok`.
- Контракт нормализации зафиксирован 46 тестами в `tests/_shared/test_normalizatsiya.py`: логические значения отвергаются, `None` → умолчание, числовые строки разбираются, контейнеры извлекаются корректно.
- Рефакторинг 8 клиентских модулей на общий модуль нормализации: Госдума, Росстат, Закупки, КАД Арбитр, ФНС, Росводресурсы, Росреестр, Росгидромет.
- Устранены ошибки null-обработки в Закупках: `currency: null` → `"RUB"` вместо `None`; `name: null` → `""` вместо `"None"`; `year: null`/`positionsCount: null` → `0` вместо падения Pydantic.
- Устранены ошибки null-обработки в КАД Арбитр: `Status: null`/`Judge: null` → `""` вместо `None` в Pydantic-полях; `ClaimSum: 0` обрабатывается корректно (ранее `if summa_syraya:` пропускал нулевую сумму); поля актов без `str()`-обёрток защищены.
- Добавлены клиентские тесты для Закупок (14 тестов в `test_client.py`): null-поля, нечисловые цены, null год/количество позиций, строковые числа.
- Добавлены клиентские тесты для КАД Арбитр (20 тестов в `test_client.py`): null статус/судья/истцы, нулевая и нечисловая сумма иска, пустые строки в истцах, плоская структура актов, ИНН в названиях сторон.
- Русифицированы имена инструментов BM25SearchTransform: `search_tools` → `poisk_instrumentov`, `call_tool` → `vypolnit_instrument`.
- Русифицированы имена инструментов CodeMode: `get_tags` → `teg`, `search` → `poisk`, `get_schema` → `poluchit_skhemy`.
- Обновлены все ссылки на `search_tools` в сообщениях об ошибках и тестах.
- Линтер, форматирование, строгая типизация (`mypy`: 0 ошибок в 217 файлах) и 780 тестов проходят без ошибок.

## Выполнено в раунде 2026-08-04 (сто шестой проход — полная строгая типизация)

- Устранены все 127 ошибок `mypy` в 18 файлах клиентов внешних API; полный `mypy src/mcp_russia/` проходит без замечаний (216 файлов).
- РосАПИ (32 ошибки): возврат приватных HTTP-обёрток типизирован как `Any`; проверка `isinstance(rezultat, Model)` вместо `isinstance(rezultat, dict)` для сужения объединённых типов в `tools.py`; параметр `dannye` аннотирован `dict[str, Any]`.
- Росгидромет (16 ошибок): обёртки `str()` для значений `object` из словарей станций; проверка `kontekst is not None` перед вызовом `.info()`.
- Закупки (15 ошибок): проверка `isinstance(elementy, list)` после извлечения из ответа; обёртки `str(... or "")` для аргументов конструкторов схем.
- КАД Арбитр (12 ошибок): проверка `isinstance(dannye_dela, dict)` и `isinstance(elementy, list)` для сужения типов; обёртки `str(... or "")`.
- Росприроднадзор (4 ошибки): уникальные имена `parametry` в парных `try`-блоках; явный `return None` в `info_proverki`.
- Совет Федерации (3 ошибки): уникальные имена `parametry`; третий элемент в кортежах справочника для соответствия заголовкам таблицы.
- Казначейство (2 ошибки): уникальные имена `parametry`; явный `return None` в `poluchit_byudzhetnuyu_smetu`.
- Росреестр (7 ошибок): `or ""` после `dict.get()` без умолчания; тип `list[dict[str, str]]` вместо `list[dict]`.
- ФНС (6 ошибок): тип `list[dict[str, str]]` вместо `list[dict]`; возврат `_poisk_egrul` типизирован как `Any`.
- ГИБДД (2 ошибки): тип `tuple[...]` вместо голого `tuple`; возврат `_izvlech_rezultat` типизирован как `dict[str, Any]`.
- ЦБ РФ (2 ошибки): возврат HTTP-обёрток типизирован как `Any`.
- Росводресурсы (3 ошибки): обёртки `str()` для значений справочника водохранилищ.
- Линтер, форматирование и 691 тест проходят без ошибок.

## Выполнено в раунде 2026-08-03 (сто пятый проход — журналирование ошибок, русификация)

- Заменены все 16 беззвучных `except Exception` в клиентах Госдумы (4), закупок (6) и КАД Арбитр (6) на `logger.exception(...)` с сохранением безопасных возвращаемых значений.
- Во все три модуля добавлены `import logging` и `logger = logging.getLogger(__name__)`.
- Устранено английское слово `relevant` в промпте модуля ЦБ РФ (`cbrf/prompts.py`): заменено на «соответствующих».
- Линтер и форматирование проходят; 82 теста трёх модулей — без ошибок.

## Выполнено в раунде 2026-08-02 (сто четвёртый проход — строгая типизация Госдумы)

- Устранены все 28 ошибок `mypy` в клиенте Госдумы; отдельная проверка восьми файлов модуля проходит без замечаний.
- Добавлены типизированные помощники для строк, целых чисел, альтернативных полей схемы и списков в корневом ответе API.
- Разбор депутатов, законопроектов и голосований больше не превращает `None` в строку `"None"` и безопасно обрабатывает отсутствующие поля, числовые строки, логические значения и неожиданные контейнеры.
- Добавлены четыре защитных клиентских теста; все 30 тестов модуля Госдумы проходят.
- Общий baseline строгой типизации уменьшен со 155 ошибок в 19 файлах до 127 ошибок в 18 файлах.
- В план добавлены фиксация общего контракта нормализации и диагностируемое журналирование ошибок внешних API.

## Выполнено в раунде 2026-08-02 (сто третий проход — строгая типизация Росстата)

- Устранены все 17 ошибок `mypy` в клиенте Росстата; отдельная проверка восьми файлов модуля теперь проходит без замечаний.
- Исправлена рассинхронизация модели и клиента: вместо несуществующего `kod_znachenie_okved` клиент заполняет поле `kod_okved`, используемое инструментами и схемами.
- Добавлена явная нормализация строк, чисел и `None` из ответов ЕМИСС без превращения отсутствующих значений в строку `"None"`.
- Добавлены клиентские тесты разбора ВРП, отраслевой структуры и инвестиций; все 54 теста модуля Росстата проходят.
- Общий baseline строгой типизации уменьшен со 172 ошибок в 20 файлах до 155 ошибок в 19 файлах.
- В план добавлены отдельные итерации по защитным тестам внешних JSON-схем и последующему выделению общей типизированной нормализации.

## Выполнено в раунде 2026-08-01 (сто второй проход — проверка документации по MCP-реестру)

- Добавлены регрессионные тесты, которые получают имена инструментов и JSON Schema параметров непосредственно из FastMCP-серверов всех модулей данных и агентов.
- Проверкой охвачены вызовы в `docs/examples`, аргументы пакетных JSON-запросов и локальные вызовы инструментов в строках `prompts.py`.
- Исправлены шесть найденных рассинхронизаций: несуществующий вызов расходов депутата и неверные параметры Росстата, Росприроднадзора, Счётной палаты и Росводресурсов.
- Девять клиентских модулей приведены к текущему формату Ruff; линтер и проверка форматирования проходят на всех 307 файлах.
- В `README.md` отражены завершённые проверки, консолидация плана и новые направления автоматизации.

## Выполнено в раунде 2026-07-29 (сто первый проход — консолидация TODO, актуализация метаданных)

- Исправлено описание проекта в `pyproject.toml`: «Русскоязычная адаптация MCP-сервера» → «Русскоязычный MCP-сервер для государственных и публичных данных РФ» — устранена формулировка «адаптация», позиционирующая проект как производную.
- Добавлены русские переводы португальских терминов в `CHANGELOG.md`: Plataforma Nacional de Contratações Públicas → Национальная платформа государственных закупок; Partidos do Senado Federal → Партии Федерального сената; Unidades Federativas do Senado → Федеративные единицы Сената; Jurisprudência do CNJ → Судебная практика Национального совета юстиции; Tribunais Regionais Eleitorais → Региональные избирательные суды; Tribunais de Justiça Militar → Суды военной юстиции.
- Консолидирован `TODO.md`: исторические отчёты раундов 67–100 перенесены в `docs/TODO_ARCHIVE.md`; в `TODO.md` оставлен только актуальный план задач.

## Статус русификации

Кодовая база полностью русифицирована. Оставшиеся английские идентификаторы — только:
- Строковые ключи API-ответов (`.get("key")`)
- Keyword-аргументы внешних библиотек (httpx, Pydantic, FastMCP)
- Стандартные Python-идентификаторы (`*args`, `**kwargs`, `__aexit__(*exc)`)
- Параметры stdlib-переопределений (`tag`, `attrs` в HTMLParser)
- Loanwords идентичные русским (`data` = «дата», `period` = «период»)
- `logger` (стандартная конвенция Python)
- CSS-классы внешних HTML-страниц (изменение сломает парсинг)
- Имена переменных окружения (внешняя конфигурация)
- Международные аббревиатуры (`WMO`, `VIN`, `API`)
- Имена инструментов BM25/CodeMode теперь русифицированы (`poisk_instrumentov`, `vypolnit_instrument`, `teg`, `poisk`, `poluchit_skhemy`)

## Архив

Исторические отчёты раундов 67–100 доступны в [docs/TODO_ARCHIVE.md](docs/TODO_ARCHIVE.md).
