"""Промпты модуля Ростехнадзора."""

from __future__ import annotations

from fastmcp import Context


async def analiz_intsidenta(kontekst: str, kontekst_mcp: Context) -> str:
    """Анализ инцидента на опасном производственном объекте.

    Аргументы:
        kontekst: Контекст запроса (например, «анализ аварии на химическом заводе»).

    Возвращает:
        Шаблон промпта для анализа инцидента.
    """
    return (
        f"Выполни анализ инцидента на ОПО.\n\n"
        f"Контекст: {kontekst}\n\n"
        f"Инструкция:\n"
        f"1. Получи данные об инцидентах через poisk_intsidentov()\n"
        f"2. Определи вид и класс инцидента\n"
        f"3. Оцени масштаб и последствия\n"
        f"4. Проверь лицензии организации через poisk_litsenziy()\n"
        f"5. Дай рекомендации по предотвращению\n\n"
        f"Важно: обращай внимание на класс опасности объекта и количество пострадавших."
    )


async def obzor_promyshlennoy_bezopasnosti(kontekst_mcp: Context) -> str:
    """Обзор состояния промышленной безопасности.

    Возвращает:
        Шаблон промпта для обзора промышленной безопасности.
    """
    return (
        "Подготовь обзор состояния промышленной безопасности в РФ.\n\n"
        "Инструкция:\n"
        "1. Получи статистику инцидентов через poisk_intsidentov()\n"
        "2. Проанализируй распределение по видам и регионам\n"
        "3. Отметь наиболее опасные отрасли\n"
        "4. Сравни с предыдущими периодами\n"
        "5. Оформи как аналитическую справку\n\n"
        "Важно: указывай виды инцидентов с наибольшим числом пострадавших."
    )
